version: '3.8'

services:
  social-site-demo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"       # maps container port 80 (nginx) to host port 80
    restart: unless-stopped
